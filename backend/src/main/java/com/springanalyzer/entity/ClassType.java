package com.springanalyzer.entity;

public enum ClassType {
    CONTROLLER,
    REST_CONTROLLER,
    SERVICE,
    REPOSITORY,
    ENTITY,
    COMPONENT,
    CONFIGURATION,
    INTERFACE,
    ENUM,
    OTHER
}
